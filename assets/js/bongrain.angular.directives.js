;(function(window, document, undefined) {
	var Bongrain = new namespace("Bongrain");

	// Overlay Directive
	/*
	Bongrain.Angular.directive('productlistModal', ['$rootScope', function($rootScope) {
		return {
			restrict: 'C',
			scope: {
				selectzilla: "@"
			},
			template:
				'<div class="productlist__modal">' +
					'<div class="productlist__modal__headline></div>"' +
				'</div>'
			,
			replace: true,
			controller: ['$scope', '$element', '$attrs', function($scope, $element, $attrs) {
				// Default Data
				if ($scope.data === undefined) $scope.data = {};
				if ($scope.data.states === undefined) $scope.data.states = {};

				//$scope.data.states.open = true;

				// Scope Functions

				// Directive Functions

			}],
			compile: function(tElement, tAttrs, transclude) {
				return {
					pre: function preLink(scope, iElement, iAttrs, controller) {

					},
					post: function postLink(scope, iElement, iAttrs, controller) {

					}
				}
			}
		}
	}]);
	*/


})(window, window.document);